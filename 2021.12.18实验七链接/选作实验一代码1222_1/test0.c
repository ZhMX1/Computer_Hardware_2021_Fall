#include<stdio.h>
#include<math.h>
#include<ctype.h>
#include<stdlib.h>
#include<string.h>
#define INF 0x3f3f3f3f
#define max 1000050
#define myType node
#define max(a,b) (((a) > (b)) ? (a) : (b))
#define min(a,b) (((a) < (b)) ? (a) : (b))
typedef struct link{
    
}node;
int main()
{
    printf("hello world, %d", 1);
    
    return 0;
}